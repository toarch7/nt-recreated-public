function screen_begin() {
    //surface_set_target(screen)

    // this draws the surface on the screen
    /*surface_reset_target();
	if UberCont.paused = 0
	draw_clear(0);
	    gpu_set_blendmode_ext(bm_one, bm_zero); // According to ChevyRay, this should fix the black box glitch
	    draw_surface_stretched(screen,screen_x,screen_y,screen_w*screen_scale,screen_h*screen_scale);
	    gpu_set_blendmode(bm_normal); // According to ChevyRay, this should fix the black box glitch
	screen_refresh();




/* end screen_begin */
}