event_inherited()

if sprite_index != spr_hurt motion_add(direction, 0.5)

if speed > 1 + rage * 2 speed = 1 + rage * 2
if speed < 0.5 speed = 0.5

if hspeed > 0 right = 1
else if hspeed < 0 right = -1